node_modules/
.env
.env.development
.env.staging
.env.production
.env*.local

# Firebase native config (per-environment API keys/project IDs — never committed)
/firebase-config/
# ...and the copies `expo prebuild` drops into the native dirs from the above
android/app/google-services.json
ios/**/GoogleService-Info.plist
.DS_Store
dist/
build/

# Expo
.expo/
web-build/
expo-env.d.ts

# Native (generated via `expo prebuild`, committed per CNG convention — see README)
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.kotlin/

# Metro
.metro-health-check*

# Firebase emulator
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# debug logs
npm-debug.*
yarn-debug.*
yarn-error.*

# typescript
*.tsbuildinfo
