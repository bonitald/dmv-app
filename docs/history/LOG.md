### 2026-09-19
- Initial state: Expo + TypeScript app is scaffolded (custom dev client, CNG workflow) with Firebase (`@react-native-firebase/*` native SDK) for Firestore/Auth/Analytics; `android/` is prebuilt and committed, `ios/` is not yet (Windows can't run the iOS prebuild step).
- Initial state: App code so far is a skeleton — `src/auth/AuthProvider.tsx`, `src/navigation/RootNavigator.tsx`, a `PlaceholderScreen.tsx`, and `src/theme/tokens.ts` — phase 0/early-phase-1 user stories (auth wiring, navigation shell, base theme).
- Initial state: `firestore.rules` + `firestore-tests/rules.test.ts` exist; a `scripts/question-bank/` ingestion CLI (chunk plan read/write, question writer, admin Firestore connection, full Jest coverage) is built out.
- Initial state: PRD (`docs/prd.md`) and phased roadmap (`docs/phases.md`) are written; phase 0 (foundations) and phase 1 (question bank & content model) have planning docs, phase 2 (flashcard study mode) user stories were just broken out — but no flashcard/study-mode screens exist in `src/` yet, so phase 2 work hasn't started in code.
- Initial state: Root `CLAUDE.md` still says "no application code" — that's stale and should be updated to reflect the real app/Firebase/scripts setup.
- Set up `docs/history/LOG.md` and the global `project-history` skill for gap-analysis on resume.
- Validated phase 1 status against actual code: us-2/us-6 confirmed complete, us-3/4/5 confirmed genuinely not started (no `functions/` dir, no test cache, no async-storage dep).
- Closed ph-1-us-1: added rules tests locking in deny-all client reads of `questions`/`ingestionRuns`; marked us-1 Complete in its story doc and phase-1-summary.md.
- Built ph-1-us-4: scaffolded `functions/` (first Cloud Function in the project) and implemented `assembleTest` — an authenticated, randomized, bounded-set callable that's now the only sanctioned read path for the `questions` collection. TDD'd via `npm run test:functions`; marked us-4 Complete.

### 2026-09-20
- Agent-assisted review of the question bank against DR_2337: 21 of 45 chunks (617 questions) done, ~55% reworded for verbatim handbook copying, 3 concepts flagged for content decisions; added qb:export-questions / qb:apply-review tooling; paused at user request.
- Added the `cloud-function-documentation` project skill and retrofitted `functions/src/assembleTest.ts` with doc/inline comments (comments only; tsc passes, jest not run — needs emulator).
- Reworked the first-run teen journey (baseline test vs. learn-first path, mini-quizzes, test-date countdown, optional account linking on anonymous auth): added a Goal/Supports-PRD note to every phase in `docs/phases.md`, a new Phase 9, and matching updates to `docs/prd.md`.
- Revised Phase 1 stories: added ph-1-us-7 to us-11 (mini-quiz assembly, fixed 45-question baseline that doubles as the free test, topic catalog, flashcards, `scoreTest`), widened us-3, and updated the phase summary. Docs only — no code changed yet; open items include narrowing the `users/{uid}/**` rules wildcard.

### 2026-09-23
- Recovered and merged prior session's worktree work into `ph2-stories`: Tasks 1-4 of the Phase 1 remaining-stories plan (shared `shuffle` helper extracted from `assembleTest`, `assembleTest` now returns `chunkId` and persists `testAssignments`, `firestore.rules` narrowed to explicit server-owned paths under `users/{uid}`, and the new `assembleMiniQuiz` callable for topic-scoped mini-quizzes) were already committed on the `worktree-phase-1-remaining-stories` worktree branch; fast-forwarded `ph2-stories` onto it and committed the previously-untracked plan doc. Left off before Task 5 (topic catalog population script, ph-1-us-9) of `docs/superpowers/plans/2026-09-22-phase-1-remaining-stories.md`.
- Phase 1 plan Tasks 5-7 done: `qb:publish-topics` (topic catalog, ph-1-us-9), Task 6 rules checkpoint, and `qb:build-baseline`/`qb:validate-baseline` (ph-1-us-8 part 1); question-bank suite 39/39. Next: Task 8, `startOrResumeBaseline` callable.
- Phase 1 plan Task 8 done: `startOrResumeBaseline` callable (ph-1-us-8 part 2) serves the fixed 45-question baseline 15 at a time from server-side progress; functions suite 21/21. Next: Task 9, `getFlashcards`.
- Phase 1 plan Task 9 done: `getFlashcards` callable (ph-1-us-10) returns a topic's approved cards with answers, rate-limited to 30 calls/10 min per user; functions suite 28/28. Next: Task 10, `scoreTest`.
- Phase 1 plan Task 10 done: `scoreTest` callable (ph-1-us-11) grades practice tests, mini-quizzes (80% move-on/review-again) and baseline sections; closed a gap in the plan where a client could score a baseline section from another version. Functions suite 39/39. Next: Task 11, frontend `testCache.ts`.
- Phase 1 plan Tasks 11-12 done, plan complete: added `src/study/testCache.ts` (AsyncStorage, `npm run test:app`) and updated phase 1 story statuses against the code — 8 of 11 Complete; us-3/us-5 await the Phase 3 test screen, us-11 still needs per-question results with correct answers in `scoreTest`. All suites green (98 tests).
- Closed ph-1-us-11: `scoreTest` now returns (and saves on the attempt) per-question results with correct answers; phase 1 is 9 of 11 Complete, us-3/us-5 await the Phase 3 test screen. Functions suite 41/41.
- `scoreTest` now withholds baseline answers until the final section, which returns `baselineReview` for all 45 questions; per-topic totals still show right/wrong per question (one question per topic). Functions suite 42/42.
- Cleanup fixes: root tsconfig excludes test files (whole-app `tsc` now clean), deleted questions no longer break `scoreTest`/`startOrResumeBaseline`, mini-quiz test cleanup fixed, `qb:build-baseline` reports the fact/scenario mix (closes last us-8 task). All suites green: app 5, rules 15, functions 44, question-bank 40.
- Closed Phase 1: moved the unfinished ph-1-us-3/us-5 criteria (test-taking screen using `src/study/testCache.ts`, plus storing the active `testId`) to new story ph-3-us-1 in `docs/phases/phase-3-practice-test-generator-and-test-taking-ui/`; Phase 1 marked Done. Next: break out the rest of Phase 3 (continue from ph-3-us-2) or start Phase 2.
- Evaluated Phase 2 against the code: ph-2-us-2 is mostly superseded by `getFlashcards` + `topics` (ph-1-us-9/10), the four learning-path bullets in phases.md have no stories yet, and `@react-native-firebase/functions` isn't installed; proposed an implementation plan with open decisions. No docs or code changed.
- Filled out Phase 2 stories: marked ph-2-us-2 superseded by `getFlashcards`/`topics`, rewrote ph-2-us-3 against the real contract (tap-a-choice, session cache, rate-limit/empty states), tightened us-4/5/6, and added ph-2-us-7 to us-12 for the concept learning path (concept list, mini-quiz, recommendation, Concept Progress split: `scoreTest` writes score, client writes status). Docs only. Next: journey-order the remaining stories.
- Changed Phase 2 flashcards to flip cards for memorizing (front = question only, back = correct answer only, "Got it"/"Still learning" self-mark feeds weak-card tracking); choices now appear only in the mini-quiz/test card. Updated ph-2-us-3/4/5/6/8 and the summary.
- Dropped per-card "Got it/Still learning" from Phase 2 flashcards: users flip through the whole concept, then choose "Take the mini-quiz" or "Start this concept over"; weak-card ordering now uses mini-quiz misses (rewrote ph-2-us-4/6, updated us-3/8/10 and the summary).
- Wrote Phase 3 stories ph-3-us-2 to us-14: practice-test mix + repeat-avoidance in `assembleTest`, the shared test-taking runner (ph-3-us-4, also used by the baseline and mini-quiz), timed mode (saved-start-time countdown + `timing` on attempts), Colorado-only guardrail, and the baseline flow UI (intro, "keep going?" check-in, resume); us-10/us-14 marked Complete as delivered by Phase 1. Docs only.
- Recorded the real CO written test format (25 questions, 60 min, 20/25 = 80% to pass) in prd.md Section 9 and resolved the per-test count question; timed mode is now 60 min (ph-3-us-3/7/9, Phase 3 summary, functions README).
- Wrote Phase 4 stories ph-4-us-1 to us-7 in docs/phases/phase-4-scoring-and-review/: practice results with pass/fail at 20/25, `scoreTest` saves question text/choices/optional explanation on attempts (only backend change), review screen, missed-questions list (also feeds Phase 2 weak-card data), per-concept strength report + baseline results screen, and score-vs-baseline; linked the Phase 2/3 placeholders to them. Docs only.
- Wrote Phase 9 stories ph-9-us-1 to us-10 in docs/phases/phase-9-first-run-onboarding-and-account-linking/: onboarding flow and gate, profile `onboarding`/`testDate` fields with tightened `users/{uid}` rules, test-date countdown, a new Home tab (first of five), and Google/Apple linking + Settings + a `deleteAccount` callable. Noted the dev smoke test in App.tsx is denied by the narrowed rules. Docs only.
- Session wrap-up: Phase 2 (updated), Phase 3, Phase 4 and Phase 9 user stories are written; all docs, uncommitted on `phase-2-features`. Added the minors privacy review as an action in prd.md Section 9 and a gate on Phase 8 in phases.md. Next: order the stories into journey-based build slices (first launch → baseline → results → learning path → practice tests → account linking).

### 2026-09-24
- Ordered the Phase 2/3/4/9 stories into six user-journey build slices in docs/build-order.md (first launch → baseline → results → learning path → practice tests → save progress); split ph-4-us-6 across Slices 3/4b/5a, moved jest-expo setup to onboarding; linked from phases.md. Docs only. Next: start Slice 1 (ph-9-us-2).
- Built Slice 1 (first launch): tightened `users/{uid}` rules for `onboarding`/`testDate` (+20 rules tests), removed the dev smoke test, onboarding gate + welcome/choice/test-date flow, Home tab (countdown, baseline and study cards, Settings with test date), JS calendar picker, jest-expo component tests (`npm run test:components`). Uncommitted; next: deploy rules, device-test Slice 1, then Slice 2 (ph-3-us-4).
